{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Build and Debug Rust App",
            "program": "${workspaceFolder}/target/debug/sahuagin",
            "args": [],
            "cwd": "${workspaceFolder}",
            "sourceLanguages": [
                "rust"
            ],
            "preLaunchTask": "Pre-launch Tasks"
        }
    ]
}